services:
  api:
    build: .
    ports:
      - "8000:8000"
    command: uvicorn --app-dir src calendar_prioritizer.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - .:/src/calendar-prioritizer
